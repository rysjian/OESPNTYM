FROM alpine:latest
RUN wget -c -t 0 --timeout=60 --waitretry=60 -O vscode-linux-arm64.tar.gz "https://code.visualstudio.com/sha/download?build=stable&os=linux-arm64"
RUN tar -xzf vscode-linux-arm64.tar.gz
RUN cp VSCode-linux-arm64 /
ENTRYPOINT ["code"]
